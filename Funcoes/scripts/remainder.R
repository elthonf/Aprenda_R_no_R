# Deixe-me mostrar um exemplo de uma função que vou invocar chamada increment().
# Na maior parte do tempo, quero usar essa função para aumentar o valor de um
# número em um. Esta função terá dois argumentos: "number" e "by" onde "number"
# é o dígito que eu quero incrementar e "by" é o valor que eu quero incrementar em "number". Veja o código abaixo.
# 
# increment <- function(number, by = 1){
#     number + by
# }
# 
# Se você observar entre os parênteses, verá que defini "by" igual a 1.
# Isso significa que o argumento "by" terá o valor padrão de 1.
# 
# Agora posso usar a função de incremento sem fornecer um valor para "by".
# A expressão: increment (5) deve retornar 6.
# 
# No entanto, se eu quiser fornecer um valor para o argumento "by", ainda posso!
# A expressão: incremento(5, 2) deve retornar 7.
# 
# Você vai escrever uma função chamada "remainder". remainder() terá
# dois argumentos: "num" e "divisor", onde "num" é dividido por "divisor"
# e o restante é retornado. Imagine que você geralmente quer saber o resto
# quando você divide por 2, então defina o valor padrão de "divisor" como 2.
# Por favor, tenha certeza que "num" é o primeiro argumento e "divisor"
# é o segundo argumento.
# 
# 
# Dica 1: Você pode usar o operador de módulo %% para encontrar o resto
#   Ex: 7 %% 4 deve retornar 3.
# 
# Lembre-se de definir o valor padrão do parâmetro divisor!
# Certifique-se de salvar este script e digite submit() no console depois de gravar a função.


remainder <- function(num, divisor) {
  # Write your code here!
  # Remember: the last expression evaluated will be returned! 
}
