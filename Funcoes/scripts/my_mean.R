# Você está livre para implementar a função my_mean como quiser, contanto que
# retorna a média de todos os números em `my_vector`.
# 
# 
# Dica 1: sum() retorna a soma de um vetor.
# 	Ex: sum(c(1, 2, 3)) deve retornar o valor 6
# 
# Dica 2: length() returna o tamanho de um vetor.
# 	Ex: length(c(1, 2, 3)) deve retornar o valor 3
# 
# Dica 3: A média de todos os números em um vetor é igual à soma de
#      todos os números no vetor divididos pelo tamanho do vetor.
# 
# Nota para aqueles que se sentem super inteligentes: Por favor,
# não use a função mean() enquanto escreve esta função. Estamos
# tentando te ensinar algo aqui! Se sabotar não vai te ajudar.
#        
# Certifique-se de salvar este script e digite submit() no console depois de fazer as alterações.

my_mean <- function(my_vector) {
  # Write your code here!
  # Remember: the last expression evaluated will be returned! 
}
